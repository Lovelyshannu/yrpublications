<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>YR Publication - <%= title %></title>
  <link rel="stylesheet" href="/css/style.css" />
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet" />
</head>
<body>
    <div class="page-wrapper"> <!-- 👈 New wrapper starts -->
  <header>
    <nav class="navbar">
     <div class="logo" style="display: flex; align-items: center; gap: 10px;">
      <a href="/"><img src="/images/logo.png" alt="YR Publication" style="height: 50px;" /></a>
      <span class="company-name">YR Publication</span>
    </div>
      <ul class="nav-links">
        <li><a href="/" class="<%= active === 'home' ? 'active' : '' %>">Home</a></li>
        <li><a href="/articles" class="<%= active === 'articles' ? 'active' : '' %>">Articles</a></li>
        <li><a href="/about" class="<%= active === 'about' ? 'active' : '' %>">About</a></li>
        <li><a href="/contact" class="<%= active === 'contact' ? 'active' : '' %>">Contact</a></li>
        <li><a href="/certificates" class="<%= active === 'certificate' ? 'active' : '' %>">Verify Certificate</a></li>
        <% if (!user) { %>
          <li><a href="/login" class="<%= active === 'login' ? 'active' : '' %>">Login</a></li>
          <li><a href="/register" class="<%= active === 'register' ? 'active' : '' %>">Register</a></li>
        <% } else { %>
          <% if(user.isAdmin) { %>
            <li><a href="/admin" class="<%= active === 'admin' ? 'active' : '' %>">Admin</a></li>
          <% } else { %>
            <li><a href="/articles/upload" class="<%= active === 'upload' ? 'active' : '' %>">Upload Article</a></li>
          <% } %>
          <li><a href="/logout">Logout (<%= user.name %>)</a></li>
        <% } %>
      </ul>
    </nav>
  </header>
  <main>
   <% if(success_msg && success_msg.trim().length > 0) { %>
  <div class="alert success"><%= success_msg %></div>
<% } %>
<% if(error_msg && error_msg.trim().length > 0) { %>
  <div class="alert error"><%= error_msg %></div>
<% } %>
<% if(error && error.trim().length > 0) { %>
  <div class="alert error"><%= error %></div>
<% } %>

